meta {
  name: UpdateByPage
  type: http
  seq: 3
}

patch {
  url: {{baseUrl}}/progress/1/page
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  { "page": 150 }
}

settings {
  encodeUrl: true
}
